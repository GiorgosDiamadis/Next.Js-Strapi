'use strict';

/**
 * compatible-device service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::compatible-device.compatible-device');
