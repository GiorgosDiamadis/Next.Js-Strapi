'use strict';

/**
 *  compatible-device controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::compatible-device.compatible-device');
